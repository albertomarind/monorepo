import { ExtraerPropiedadPipe } from './extraer-propiedad.pipe';

describe('ExtraerPropiedadPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtraerPropiedadPipe();
    expect(pipe).toBeTruthy();
  });
});
