import { BitacoraDirective } from './bitacora.directive';

describe('BitacoraDirective', () => {
  it('should create an instance', () => {
    const directive = new BitacoraDirective();
    expect(directive).toBeTruthy();
  });
});
